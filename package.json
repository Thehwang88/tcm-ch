{
  "name": "tcm-ch",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/gen-sitemap.mjs && wrangler pages functions build --outdir=./worker/",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.6.0"
  },
  "devDependencies": {
    "puppeteer": "^25.1.0",
    "purgecss": "^8.0.0",
    "wrangler": "^4.105.0"
  }
}
